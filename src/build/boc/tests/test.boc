include ../../test.boc;

var $(BOC_OPTS);
var $(BOC_NO_PROGRESS)=--no-progress;

function run_boc(opts)
{
    $(BOC)
        $(opts)
        $(BOC_NO_PROGRESS)
        >>$(TESTSUITE_DIR)/stdout
        2>>$(TESTSUITE_DIR)/stderr
        || true;
}
