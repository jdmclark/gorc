include ../test.boc;

cp testproject.json $(TESTSUITE_DIR)/testproject.json;
pushd $(TESTSUITE_DIR);

$(BOC_BUILD) --override-root-name testproject.json
             --override-cache-name notadir/project.cache 2>>$(RAW_OUTPUT);

popd;

call process_raw_output();
call compare_output();
