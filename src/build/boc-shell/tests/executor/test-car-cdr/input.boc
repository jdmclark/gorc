var $(LIST) = a b c;

if(head($LIST) == a) {
    echo "First argument matches";
}

if(head(tail($LIST)) == b) {
    echo "Second argument matches";
}

if(head(tail(tail($LIST))) == c) {
    echo "Third argument matches";
}
