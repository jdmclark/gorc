var $(list) = (((e @ f) @ (g @ h)) @ ((c @ (a @ b)) @ d));

function print_list(list)
{
    if null($list) {
        return;
    }

    echo (head($list));
    call print_list(tail($list));
}

call print_list($list);
