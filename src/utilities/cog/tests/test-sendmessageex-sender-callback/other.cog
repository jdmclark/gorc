symbols
message user0
end
code
user0:
    print("inside other");
    print("senderref:");
    printint(getsenderref());
    print("sendertype:");
    printint(getsendertype());

    sendmessageex(getsenderref(), user1, 0, 0, 0, 0);
end
