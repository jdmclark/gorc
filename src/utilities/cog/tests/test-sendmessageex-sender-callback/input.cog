symbols
cog other
message startup
message user1
end
code
startup:
    print("sending message to other");
    sendmessageex(other, user0, 0, 0, 0, 0);
    return;

user1:
    print("received callback");
    return;

end
