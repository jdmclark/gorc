symbols
message startup
int x
int y
end
code
startup:
    x + y;
    x - y;
    x * y;
    x / y;
    x % y;
    x & y;
    x | y;
    x ^ y;
    x || y;
    x && y;
    x < y;
    x <= y;
    x > y;
    x >= y;
    x != y;
    x == y;
end
