symbols
message startup
int x
int y
int z
end
code
startup:
    z = x + y;
    z = x - y;
    z = x * y;
    z = x / y;
    z = x % y;
    z = x & y;
    z = x | y;
    z = x ^ y;
    z = x || y;
    z = x && y;
    z = x < y;
    z = x <= y;
    z = x > y;
    z = x >= y;
    z = x != y;
    z = x == y;
end
