symbols
int i
flex T
flex F
end
code

startup:
	T = (i == i);
	F = (i != i);

	if(T)
	{
		PrintInt(0);
	}

	if(!T)
	{
		PrintInt(1);
	}

	if(!!T)
	{
		PrintInt(2);
	}

	if(F)
	{
		PrintInt(3);
	}

	if(!F)
	{
		PrintInt(4);
	}

	if(!!F)
	{
		PrintInt(5);
	}

end
